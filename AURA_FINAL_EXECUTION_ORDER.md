# AURA 최종 실행 지시서
## MODEL SCOUT 인수인계 기반 / 확정본 v1.1

## 1. 목적과 범위

MODEL SCOUT의 `SCOUT-GATE-04` 완료 상태를 전제로, 이제부터 작업 범위는 **AURA 적용 실행(P03~P11)** 으로 한정한다.

MODEL SCOUT의 기능 개발, 게이트 재검증, 기존 완료 항목의 재작업은 금지한다. 단, AURA 산출물의 근거를 남기기 위한 모델, 데이터셋, Space 조회와 결과 저장은 허용한다.

판단 근거는 다음 문서와 실행 구성만 인정한다.

- `CURRENT_EXECUTION_CONTROL.md`
- `README.md`
- `requirements.txt`
- `run_web_ui.bat`
- `run_web_ui.ps1`
- 기존 AURA PR #14 인수인계 방식
- 본 지시서 이후 생성되는 증적 산출물

## 2. 변경 금지 규칙

1. `LICENSE_NOT_PERMITTED`, `LICENSE_REVIEW_REQUIRED`, `REJECT` 상태의 후보는 추천, 선정, PDF 반영 대상에서 즉시 제외한다.
2. 추천에는 반드시 쿼리, 필터, 라이선스 상태, 비교 결과, 추천 사유를 남긴다.
3. 조회 `limit`은 반드시 `1~100` 범위로 유지한다.
4. 동일 ID라도 `(resource_type, model_id)`가 다르면 별도 후보로 판정한다.
5. "SCOUT 완료"와 "AURA 반영 완료"를 혼동하지 않는다.
6. 근거가 없는 성능, 라이선스, 상용 이용 가능 주장은 금지한다.
7. 단계별 산출물의 파일명, 저장경로, 생성 시각, 해시를 기록한 뒤 다음 단계로 넘어간다.

## 3. 실행 절차

### P03. 적합 후보 확정

- Model, Dataset, Space를 모두 조회한다.
- 상용 이용 및 라이선스 조건을 통과한 후보만 1차 목록에 넣는다.
- JSON과 Markdown 형식의 조회 결과를 `artifacts/`에 저장한다.

### P04. 후보 비교와 1차 추천

- 후보별 점수, 라이선스, 출처, 다운로드 및 활용 지표, 경고를 비교한다.
- 추천 Top 후보와 제외 후보를 명시한다.
- 제외 사유를 반드시 기록한다.

### P05. 모델 카드 근거 보강

- 선정 후보의 특징, 제약, 성능 근거, 사용 조건, 리스크를 모델 카드 기준으로 정리한다.
- 확인하지 못한 내용은 "미확인"으로 표기하며 추정으로 채우지 않는다.

### P06. 비교표 정합성 검토

- 점수, 리스크, 라이선스, 출처, 다운로드 지표를 하나의 비교표로 정리한다.
- 표의 값은 저장된 SCOUT 증적 및 모델 카드와 일치해야 한다.

### P07. AURA 제작 입력 패키지 생성

- 디자인 자산 제작에 필요한 텍스트, 표준 메타데이터, 사용 조건, 출처 표기 문구를 생성한다.
- 라이선스 표기와 사용 제한은 누락 없이 포함한다.

### P08. 내부 검토

- P03~P07 산출물을 검토한다.
- 누락, 라이선스 위험, 근거 불일치, 추적 불가 항목은 제거 또는 `BLOCK` 처리한다.

### P09. 최종 디자인 후보 확정

- P08을 통과한 후보만 최종 디자인 후보로 확정한다.
- 확정 근거와 탈락 근거를 함께 보관한다.

### P10. PDF 반영 리소스 생성

- 디자인 파일, 사용 텍스트, 캡션, 출처 및 라이선스 표기부를 PDF 반영용 패키지로 만든다.

### P11. 최종 PDF 및 제출 패키지

- 최종 PDF와 모든 근거 자료를 연결한다.
- 인수인계 체크리스트에 완료, 미완료, 차단 항목을 명시한다.

## 4. 필수 증적

GitHub PR에는 아래 항목을 반드시 포함한다.

- `AURA_FINAL_EXECUTION_ORDER.md`
- `artifacts/*.json`
- `artifacts/*.md`
- `artifacts/*-snapshot.json` (변경 추적이 필요한 경우)
- CLI 출력 또는 웹 UI 실행 증적
- 단계별 파일 경로 및 해시 목록
- 인수인계 체크리스트
- 리스크 및 차단 항목 기록
- 재현 명령과 실행 환경 정보

## 5. 조회 및 실행 기준

웹 UI는 `http://127.0.0.1:8765`에서 운영한다.

AURA 근거 생성용 조회는 JSON과 Markdown 두 형식으로 저장한다. 각 조회는 `resource=all`을 기본으로 하며, 필요 시 `model`, `dataset`, `space`를 분리해 보완한다.

SCOUT 조회는 산출물 근거 수집을 위한 것이며, 기존 게이트의 완료 상태를 변경하거나 다시 검증하는 행위가 아니다.

## 6. 즉시 중단 조건

다음 중 하나라도 발생하면 작업을 중단하고 `BLOCK`으로 기록한다.

- 라이선스 통과 후보가 없는 경우
- 웹 UI가 반복 실패하거나 서버 오류가 지속되는 경우
- 후보 변경이 반복되어 결과 재현성이 확보되지 않는 경우
- PR 문서와 실제 산출물의 해시 또는 내용이 일치하지 않는 경우
- 근거 문서 없이 후보를 확정해야 하는 경우

## 7. GitHub 보관 규칙

- 브랜치: `feat/aura-execution-order-...`
- 커밋: `chore: add AURA final execution order`
- PR 제목: `feat: add AURA execution order for MODEL SCOUT handoff`

PR 본문에는 다음을 포함한다.

- `SCOUT-GATE-04` 완료 상태 요약
- `CURRENT_EXECUTION_CONTROL.md`와 실행 문서 기반의 근거
- AURA P03~P11 진행 현황
- 증적 파일 목록
- 재현 명령
- 완료, 미완료, `BLOCK` 항목

## 8. 완료 정의

다음 조건을 모두 충족해야 완료다.

- AURA P03~P11 산출물이 단계별로 존재한다.
- 모든 최종 후보가 라이선스 게이트를 통과했다.
- 각 판단에 대한 조회, 비교, 출처 근거가 남아 있다.
- PR의 설명, 증적, 산출물 해시가 서로 일치한다.
- 인수인계자가 추가 설명 없이 다음 작업을 재현할 수 있다.

## 9. 실행자 선언

이 지시서는 채팅에서 승인된 뒤에만 실행한다. 승인 후에는 지시서, 증적, 결과물을 하나의 GitHub PR로 보관한다. 지시서 변경이 필요하면 버전을 올리고 변경 사유를 기록한 뒤 새 승인 절차를 거친다.
