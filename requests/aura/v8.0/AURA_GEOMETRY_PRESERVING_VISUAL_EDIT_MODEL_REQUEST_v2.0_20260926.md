# Model Scout Handoff v2 — Geometry-Preserving Controlled Visual Edit

Return exact model ID/revision/files/SHA, license snapshot and acquired-version persistence proof, hardware/RAM/VRAM/runtime/cost, deterministic command/settings and raw outputs.

Benchmark package contains frozen source SHA, target visual change, explicit edit mask, protected geometry/edge mask and acceptance profile. For every output record SHA, building count, layout, centroid, footprint/topology, protected-edge check, inside/outside pixel/vector diff and visual A/B. E1 outside-mask delta must equal zero.

KEEP requires deterministic geometry PASS plus visible material/landscape/light/people/atmosphere/render improvement. SUPPORT_ONLY may be used only in narrower controlled scope. Any building-count/layout/geometry/protected-edge drift, unverifiable license persistence or impractical runtime is REJECT. Qwen/FLUX/SDXL/ControlNet family names are not authorization.
